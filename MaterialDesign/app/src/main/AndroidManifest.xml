<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.materialdesign">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:name=".context.HomeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Guide">
        <activity android:name=".activity.GuideActivity"
            android:theme="@style/AppTheme.Guide"
              >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.picture.PictureActivity"
            android:theme="@style/AppTheme.Picture"></activity>
        <activity android:name=".activity.MainActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".activity.BackLashRecycleActivity"
            android:theme="@style/AppThemeBase"></activity>
        <activity android:name=".webview.WebViewActivity"
            android:theme="@style/AppThemeBase"></activity>
    </application>

</manifest>